spring.application.name=${SPRING_APPLICATION_NAME}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT-PATH}
server.port=${SERVER_PORT}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL-AUTO}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}


springdoc.swagger-ui.path=${SPRINGDOC_SWAGGER-UI_PATH}
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE}
management.endpoint.logfile.external-file = ${MANAGEMENT_ENDPOINT_LOGFILE_EXTERNAL-FILE}

spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.protocol=${SPRING_MAIL_PROTOCOL}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username= ${SPRING_MAIL_USERNAME}
spring.mail.password= ${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE}


