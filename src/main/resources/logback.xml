<?xml version="1.0" encoding="UTF-8"?>
<!-- configuration file for LogBack (slf4J implementation)
See here for more details: http://gordondickens.com/wordpress/2013/03/27/sawing-through-the-java-loggers/ -->
<configuration scan="true" scanPeriod="30 seconds">
    <property name="LOGS_BACK" value="/opt/devop7303/log" />

    <timestamp key="byDay" datePattern="yyyy-MM-dd"/>

    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <!-- To enable JMX Management -->
    <jmxConfigurator/>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} [%L] - %msg %n</pattern>
        </encoder>
    </appender>

    <appender name="SYSLOG" class="ch.qos.logback.classic.net.SyslogAppender">
        <syslogHost>localhost</syslogHost> <!-- Change to your syslog server address -->
        <port>514</port> <!-- Default syslog port -->
        <facility>LOCAL0</facility> <!-- Adjust as necessary -->
        <!-- <protocol>UDP</protocol> --> <!-- or TCP -->
        <suffixPattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} [%L] - %msg %n</suffixPattern>
        <!--        <encoder class="ch.qos.logback.classic.net.SyslogEncoder">-->
        <!--            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg %n</pattern>-->
        <!--        </encoder>-->
    </appender>

    <appender name="FILE-AUDIT"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
        </filter>
        <file>${LOGS_BACK}/log.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} [%L] - %msg %n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>${LOGS_BACK}\\log.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <!-- each file should be at most 10MB, keep 60 days worth of history, but at most 2GB -->
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>20</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>

    </appender>

    <!-- Specify logging levels -->
    <logger name="org.springframework" level="DEBUG"/>
    <logger name="org.hibernate" level="DEBUG"/>
    <logger name="com.boltonuni.devop7303" level="DEBUG">
        <appender-ref ref="FILE-AUDIT" />
        <appender-ref ref="console" />
        <appender-ref ref="SYSLOG" />
    </logger>



    <root level="ERROR">
        <!--        <appender-ref ref="FILE-AUDIT"/>-->
        <appender-ref ref="console"/>
        <appender-ref ref="SYSLOG"/>
    </root>
</configuration>
